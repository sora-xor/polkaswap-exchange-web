import type { Options } from '../types.js';
export declare function beachballIcon(
  seed: string | Uint8Array,
  { size }: Options,
  className?: string,
  style?: Record<string, string>
): HTMLElement;
