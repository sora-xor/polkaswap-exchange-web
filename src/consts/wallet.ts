/**
 * Feature switches passed to the wallet bootstrap without importing the
 * application-wide constants barrel.
 */
export const WalletPermissions = {
  addAssets: true,
  addLiquidity: true,
  bridgeAssets: true,
  createAssets: true,
  sendAssets: true,
  showAssetDetails: true,
  swapAssets: true,
};
