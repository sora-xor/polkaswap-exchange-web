/**
 * App-owned facade for the shared wallet component registry. Import this shim
 * from application code instead of the vendored component registry path.
 */
export { components } from '@/lib/soraneo-wallet/src/components/registry';
